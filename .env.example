# 環境變數範本：複製本檔為 .env 後即可生效（.env 不會進版控）。
# 程式端一律經 src/config/env.ts 讀取；新增變數時同步補 src/vite-env.d.ts 型別。

# API 基底網址（階段六後端定案後指向真後端）
# 預設使用 JSONPlaceholder 公開測試 API；換成自家後端只需修改此值。
VITE_API_BASE_URL=https://jsonplaceholder.typicode.com

# LIFF App ID（B-10 LIFF 包裝時填入；留空＝純網頁模式）
VITE_LIFF_ID=
